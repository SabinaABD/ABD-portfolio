export const FIRST_NAME = "First name";
export const LAST_NAME = "Last name";
export const ID_NO = "ID no.";

export const enum RadioButtonEnum {
  US_RESIDENT = "US resident",
  US_NOT_RESIDENT = "US not resident",
  RESIDENCE = "residence",
}

export const enum RadioButtonIDEnum {
  RESIDENT = "resident",
  NOT_RESIDENT = "not-resident",
}
